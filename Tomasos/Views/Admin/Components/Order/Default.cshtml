@model Tomasos.Models.AdminViewModels.Order

<form asp-controller="Admin" asp-action="ManageOrdersViewComponent">
      @*data-ajax="true"
      data-ajax-method="POST"
      data-ajax-mode="replace"
      data-ajax-update="#ManageOrders">*@

    <input type="checkbox" asp-for="@Model.Delivered"/>
    <input type="hidden" asp-for="@Model.KundId"/>
    <input type="hidden" asp-for="@Model.BestallningsId"/>
    <button class="btn btn-primary">Update</button>
</form>