@model Tomasos.Models.CartViewModels.CartViewModel

<div class="row justify-content-center">
    <div class="col-md-5">
        <form asp-controller="Admin" asp-action="ManageDishesViewComponent"
              data-ajax="true"
              data-ajax-method="POST"
              data-ajax-mode="replace"
              data-ajax-update="#dishes">

            <div class="form-group">
                <select asp-for="DishId" asp-items="@Model.DishList" class="form-control">
                </select>
            </div>
            <div class="form-group">

            </div>

            <button type="submit" class="btn btn-default">Hej</button>
        </form>
    </div>
</div>